# 1:1 선택형 상담 구현

## 구현된 기능
- 기존 네 캐릭터, 남색·금색 디자인 유지. 홈과 결과 하단에서 /chat 진입.
- 6개 분야/24개 주제 → 상황 → 관심 → 시점 → 답변. 총 5단계.
- 선택을 바꾸면 이후 선택을 초기화. 추가 고민 300자, 생성 전 확인.
- 답변은 핵심·해석·실천·주의·자기 성찰 질문으로 구분. 이전 상담 다시 읽기.
- Next 운영 UI와 비공개 샘플이 같은 Consultation 컴포넌트를 사용.
- POST /api/chat: NextAuth 세션 및 출처 확인 → 본인 소유이며 이용 권한이 검증된 사주 조회 → 허용된 세 선택 검증 → 기존 엽전 원장 예약 → Claude 1회 → 구조 검증 → 저장 확정 → 응답.
- 모델 자동 재시도 0, 출력 최대 1600토큰, 추가 고민 300자. 사주 핵심 필드만 전달하고 12개 본문/전체 대화 이력을 보내지 않음.
- 동일 입력·같은 날 재요청은 저장본 반환. 동시 생성 1개, 10분당 최대 6회 시도, 하루 완료 상담 최대 5건.
- 기존 원장으로 실패 예약 반환/중복 차감 방지/환불 연동. 완료 답변은 본인 계정 조회만 허용.

## 운영 활성화 전
운영 Vercel, Supabase, 토스 설정은 변경하지 않았다. 기존 ANTHROPIC_API_KEY를 사용하는 코드가 준비되어 있지만 실제 모델 응답 및 실제 결제는 아직 운영에서 검증하지 않았다.
1. staging에 기존 launch-security, refunds 마이그레이션 이후 supabase-chat.sql 적용.
2. 1회 1냥은 검토용 구현 기준이다. 사용자 요금 승인 후 CHAT_COST_COINS=1, CHAT_ENABLED=true를 설정한다. 두 값과 API 키가 준비되지 않으면 실제 요청은 차감 전에 거절된다. CHAT_MODEL은 기존 서비스와 같은 claude-sonnet-4-6이 기본이며 서버에서 변경 가능.
3. staging에서 두 계정으로 본인/타인 사주 접근, 실제 모델 출력, 생성 실패·응답 유실·환불 후 접근을 확인한다.
4. 사용량은 기존 ai_usage에 입력·출력 토큰과 모델을 저장. 신규 상담 요금 변경 시 원장의 단위와 UI/API를 함께 변경해야 하며 환경변수만 임의로 2 이상 바꾸지 않는다.

## 비공개 샘플의 범위
예시 계정과 사주를 사용하며, 선택에 따라 사전 작성 문장을 조합한다. 화면에 예시임을 표시한다. 실제 AI 호출·계정 정보 전송·결제 없음. 상담 기록은 해당 브라우저에만 저장되며 운영용 기록은 Supabase readings에 저장하도록 구현했다. 샘플 상태 체험에서 실패와 초기화를 확인할 수 있다.

## 검증
- 24개 주제 × 4개 캐릭터 입력 검증, 잘못된 선택/문자 수/동의/추가 단계 거절.
- 출력 구조·불완전 응답 거절, 비공개 사주 필드 축약.
- PGlite에서 SQL 재실행, 잔액 부족, 동시 요청, 실패 복원, 재조회 무차감, 요청 변경 거절, 계정 격리, 하루 5회 및 날짜 변경, RLS 확인.
- 기존 보안 및 환불 테스트 통과. Next 및 Vite 빌드 통과.
- 모바일 브라우저에서 5단계, 뒤로 가기/선택 변경, 기록 재조회, 실패 뒤 재시도 검증. 320px/390px 가독성 확인.
- 실제 고객 계정 및 결제수단으로 검증했다는 뜻은 아니다.
