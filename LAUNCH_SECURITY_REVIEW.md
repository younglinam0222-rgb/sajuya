# 사주궁 출시 전 결제·권한 수정 검수 기록

2026-09-14 / 분리 브랜치 `fix/launch-access-payments`

운영 Vercel, 운영 Supabase, 토스 설정, 비공개 Sites 배포를 변경하지 않았다. 현재 코드는 출시 전 검토용이다.

## 구현한 범위

- 두 결과 조회 URL 모두 로그인과 소유자를 검사한다. `access_verified`와 이용권 확인 전에는 해석 본문을 반환하지 않는다.
- 클라이언트가 보낸 `isPaid`, 결과 본문, 완료 표시를 저장 권한의 근거로 사용하지 않는다. 결과는 서버에서 완성·검증한 뒤 저장한다.
- `supabase-launch-security.sql`은 브라우저 DB 직접 접근을 차단한다. NextAuth 로그인 확인을 통과한 서버만 서비스 역할로 접근한다.
- 모든 6개 생성 API는 DB 예약을 거친다. 유료 생성은 1냥, 일일운세는 계정당 최초 1회 무료다. 무료 사용 후 일일운세의 추가 판매 정책은 임의로 만들지 않았고 신규 생성을 막는다.
- 동일 사용자·상품·입력·기준 기간의 완료 결과를 재사용한다. 서버 생성본을 다시 볼 때 AI를 호출하지 않는다. 사주 등은 연도별, 일일운세는 한국 날짜별로 캐시를 구분한다.
- 사용자 행 잠금으로 동시 생성·중복 차감을 방지한다. 실패 시 예약 금액을 한 번만 반환한다. 20분 이상 중단된 작업은 다음 요청 또는 유지보수 실행으로 복구한다. 자동 스케줄은 아직 설치하지 않았다.
- 최근 10분 생성 시도 6회 제한. 계정당 한 작업만 진행한다. DB 장애 시 무료 통과시키지 않는다.
- 서버 생성 주문에 구매자·상품·금액·대상 결과·동의 버전을 저장한다. 1냥 1,900원 / 3냥 4,900원 / 기존 결과 직접 잠금 해제 4,900원 설정을 유지했다. 두 종류의 4,900원 상품은 구분되어 있다. 신규 생성은 1냥 사용 시 전체 결과 권한이 포함되므로 추가 잠금 해제 결제를 요구하지 않는다.
- 누락된 충전 준비 API와 주문별 결제 화면을 추가했다. 승인 시 토스 원본 응답의 주문·금액·통화·결제 키·완료 상태를 검사한다.
- 토스 승인에는 주문별 멱등키를 사용한다. 승인 후 DB 실패는 재조회·재처리로 복구한다. DB 지급·결과 해제는 한 트랜잭션으로 처리한다.
- 웹훅은 전달받은 상태를 신뢰하지 않고 토스에 재조회한다. 토스에서 확인한 전액 취소는 지급 취소 또는 직접 구매한 결과 권한 회수로 처리한다. 이미 사용한 환불 충전분은 음수 잔액이 될 수 있어 운영자의 정산 확인이 필요하다. 부분 취소는 자동 비례 정산하지 않으며 별도 확인이 필요하다. 환불을 실행하는 API는 만들지 않았다.
- 기존 `is_paid` 값은 사용자 조작 가능성이 있었으므로 일괄 신뢰하지 않는다. 기존 결제 원장과 토스의 독립적인 완료 증거가 일치하는 경우에만 복구하는 별도 유지보수 모드를 추가했다.
- 카카오톡 공유와 링크 복사를 유지한다. 공유 클릭 시 별도 난수 토큰을 발급하고 해석 본문만 허용 목록으로 공개한다. 입력 생년월일, 사용자 ID, 선택 질문, 내부 메타데이터를 제외한다. 본문 자체에 개인정보가 서술되어 있다면 그 문장은 공유될 수 있으므로 공유 전 화면에 본문 공개를 고지한다. 소유자가 기존 공유를 종료할 수 있다.
- 이메일이 없는 계정도 보관함을 조회하도록 사용자 ID로 조회한다. 모든 종류의 새 결과를 서버 보관함에 저장한다.
- 잘못된 무료/가입 엽전 지급 문구를 수정하고 신규 가입 엽전 자동 지급을 중단했다. 이미 보유한 잔액은 임의로 삭제하지 않았다. 출석 보상은 기존 정책을 유지하되 로그인·원자적 지급으로 바꿨다.
- 실패 메시지·결제 확인 재시도를 제공한다. `/refund`가 홈페이지 복사본을 보여주던 오류를 이용약관 환불 조항 이동으로 수정했다. 환불 약관 전체의 법률 적합성 검토를 완료한 것은 아니다.
- 각 AI 응답의 모델·입출력 토큰·캐시 토큰을 `ai_usage`에 기록한다. 본문이나 이름을 비용 로그에 기록하지 않는다. 공급자 청구서와 대조가 필요하며, 공급자가 응답을 반환하지 않은 장애·사용량 기록 DB 장애까지 비용을 완전 집계하지는 못한다.

## 로컬 검증 결과

- `npm run test:security`: 통과. PGlite PostgreSQL 엔진에서 실제 SQL 마이그레이션 2회 적용, 권한 차단, 예약 중복, 실패 환급, 무료 1회, 중단 복구, 주문 소유자, 금액·키 변경 거절, 중복 지급 방지, 잠금 해제 실패 트랜잭션 롤백, 출석 중복, 전액 취소 중복 및 재승인 차단 확인.
- 순수 정책 테스트: 상품 허용 목록, 토스 응답 일치 검사, 공유 필드 제외, 완료되지 않은 SSE/오류 거절, 12개 결과와 선택 질문 완성 검사, 캐시 재전달 확인.
- Next.js 실제 프로덕션 빌드: 테스트용 환경변수로 통과. 유효한 운영 키를 사용한 검증이 아니다.
- 실제 로컬 HTTP: 15개 인증 경로 401, 유지보수 키 검사, 잘못된 공유 토큰 404, 9개 페이지 정상 응답.
- 서명한 테스트 세션 + 격리된 REST 모형 DB: 본인 유료 본문 허용, 미검증 `is_paid=true` 본문 제외, 다른 계정 404(신·구 API 모두), 조작 저장 무쓰기, 다른 출처 요청 403 통과.
- HTTP 테스트의 REST DB는 모형이다. 실제 Supabase 연결·OAuth·Toss 위젯·카카오 SDK 테스트를 대체하지 않는다. PGlite는 실제 Supabase 다중 연결 환경의 부하 테스트를 대체하지 않는다.

## 적용 순서 — 외부 환경 변경 전 승인 필요

1. 운영 데이터 백업과 현재 배포 커밋 확인. 현재 소스가 운영과 동일한지 먼저 비교한다.
2. 별도 Supabase 테스트 프로젝트에 기존 스키마를 적용하고 `supabase-launch-security.sql`을 적용한다. 운영에 바로 붙여넣지 않는다.
3. 테스트 배포에 Supabase 서버 키, NextAuth 제공자 설정, 토스 테스트 상점 키, `NEXTAUTH_URL`, 임의의 강한 `RECONCILE_SECRET`을 설정한다. 비밀값을 코드·문서·로그에 넣지 않는다.
4. 실제 카카오·네이버 로그인, 토스 테스트 결제, 중복 클릭, 승인 직후 연결 끊기, DB 실패 후 재조회, 무료 동시 요청, 다른 계정 접근을 검증한다. 360/390/430px 및 실제 휴대폰 결제 리다이렉트와 카카오 공유를 확인한다.
5. 토스 웹훅을 `/api/pay/webhook`에 연결한다. 비밀키로 보호된 `POST /api/internal/reconcile`을 외부 스케줄러에서 주기 실행하도록 설정한다. 자동 복구는 이 연결을 완료해야 지속 동작한다.
6. 과거 구매 건은 보호된 `POST /api/internal/reconcile?legacy=1`로 토스와 대조한다. 한 실행은 최대 100개 기존 결제 행을 검토하므로 대량 이력은 별도 페이지 처리 계획이 필요하다. 주문 형식·금액이 다르거나 DB에 결제 자체가 없는 과거 건은 수동 대조 대상이다. 이 검토 없이 기존 결제자에게 재구매를 요구하지 않는다.
7. 테스트를 통과한 수정본과 기존 구매자 복구 결과를 사용자에게 제시한 후 운영 반영 승인을 받는다. 스키마와 서버를 함께 전환한다. 이전 취약 서버만 다시 배포하는 롤백은 금지한다.

## 남은 출시 판정 항목

- 실제 Supabase 테스트 프로젝트 및 Toss 테스트 상점 연동 검증.
- 운영 기존 결제·잔액 정합성, 기존 구매자 권한 복구.
- 서로 다른 소셜 계정을 동일한 사람으로 합치는 기능은 없음. 무료 정책은 계정 기준이다. 기존 제공자 사용자 ID 체계의 계정 연동은 별도 검토 대상이다.
- 실기기 모바일 화면·결제·공유, 실제 생성의 300초 제한 및 연결 중단 복구 확인.
- 실제 공급자 비용, 결제 수수료, 세금 구조, 환불률 측정 후 판매 가격 최종 결정. 990원으로 가격을 바꾸지 않았다.
- 환불 안내 및 운영 문의 대응 절차 확정. 부분 환불 자동 정산은 범위 밖이다.

## 현재 전달 방식의 의도와 제한

서버는 결과 전체를 검증하고 DB에 저장한 뒤 클라이언트에 보낸다. 기존 대기 화면은 유지하지만, 생성 중 부분 해석이 실시간으로 표시되는 동작은 완료 후 표시로 바뀐다. 중간 결과가 저장 완료로 오인되거나 결제 처리와 어긋나는 것을 막기 위한 변경이다. 추후 작업 상태 조회·부분 결과 스트리밍을 복구하려면 같은 서버 원장을 유지해야 한다.

## 참고한 토스 공식 문서

- https://docs.tosspayments.com/reference/using-api/authorization
- https://docs.tosspayments.com/reference
