# 환불 개선 작업본 · 운영 적용 전 확인

2026-09-14. 기존 가격과 서비스 구조를 유지한다. 이번 Sites 배포는 소유자만 보는 **예시 주문 시안**이며 실제 결제·환불·Supabase 연결은 없다. Vercel과 운영 데이터베이스는 변경하지 않았다.

## 확인할 화면

- `/payments`: 구매 건별 미사용분, 예상 환불액, 신청, 처리 상태와 담당자 답변.
- `/admin/refunds`: 오래된 예외 우선, 구매·사용 기록과 승인 예정액, 사유 입력 후 승인/종료, 지연 취소 재확인.
- 시안 상단에서 고객/주인장 화면 전환과 예시 초기화. 예시 상태는 해당 브라우저에만 저장된다.
- `/terms#refund`: 승인한 운영 방향에 맞춘 환불 조항 개정안. 나머지 약관 전체의 최종 확정·법률 검토를 의미하지 않는다.

## 서버 작업본의 처리 원칙

| 경우 | 처리 |
|---|---|
| 출처가 확인되는 미사용 유료 엽전 | 서버가 실제 구매가로 계산하고 엽전을 보관한 뒤 토스 취소 |
| 3냥 4,900원 중 미사용 2냥 | 3,267원, 원 미만 올림, 누적 환불액은 결제액 이내 |
| 생성 실패로 반환된 유료 엽전 | 같은 구매 출처로 반환된 사실을 확인하여 환불 가능 |
| 제공 항목 누락·내용 이의·중복 결제 주장 | 주인장 검토, 현재 금액 재확인 후 승인 |
| 이미 사용한 콘텐츠의 환불 승인 | 취소 확인 후 해당 결과의 열람·공유·캐시 권한 회수 |
| 토스 응답 또는 내부 저장 실패 | 엽전 보관 유지, 동일 요청 키와 결제 조회로 재확인 |
| 토스 관리자에서 별도로 취소 | 구매분 추가 사용 보류. 이미 예약된 환불과 금액이 같은 토스 완료 거래를 주인장이 대조·연결 가능 |
| 출처가 없는 기존 잔액·기존 직접 해석 구매 | 자동 환불하지 않고 증빙 확인. 별도 정산·이관 필요 |
| 반복 요청 | 중복은 동일 요청 반환. 24시간 내 다수 신규 요청은 검토로 전환하며 일률적으로 환불권을 부정하지 않음 |

사용 가능한 무료분을 먼저 차감한다. 환불과 해석 요청은 동일 회원 행을 잠가 같은 엽전의 동시 사용을 막는다. 사용자가 전달한 금액·회원 ID·결제키는 환불 계산에 사용하지 않는다. 최초 일일운세 무료 기록은 환불로 초기화하지 않는다.

## 필요한 운영 설정 — 아직 적용하지 않음

1. 별도 테스트 DB에서 기존 마이그레이션 이후 `supabase-launch-security.sql`, 그 다음 `supabase-refunds.sql`을 검증한다. 기존 잔액은 임의로 유료로 분류하지 않고 `legacy`로 보존한다. 기존 거래·사용 증빙을 대조하기 전에는 자동 환불 대상이 아니다.
2. 마이그레이션은 결제·생성 함수를 교체한다. 실제 적용 시 진행 중 생성과 결제를 먼저 정리하고 백업·점검 시간을 확보해야 한다. 기능 플래그만 되돌리는 것은 DB 롤백이 아니다.
3. 서버 환경 변수 `REFUNDS_ENABLED`는 기본 비활성이다. 실제 동작은 문자열 `true`일 때만 활성화한다. 관리자 세션의 회원 ID를 `ADMIN_USER_IDS`에 명시한다. 클라이언트 변수로 노출하지 않는다.
4. 기존 토스 서버 시크릿·Supabase 서비스 역할 키·NextAuth 설정을 사용한다. `NEXTAUTH_URL`은 실제 서비스 출처와 일치해야 한다. 환불 변경 요청은 로그인·출처 검증을 통과해야 한다.
5. `CRON_SECRET`을 설정하고 `Authorization: Bearer <CRON_SECRET>`을 붙인 `/api/internal/refunds` 호출을 예약해야 한다. 코드만 배포하면 스케줄은 생성되지 않는다. 작업당 최대 4건이며 운영량에 맞춰 주기를 정하고 실패를 관찰한다. 외부 스케줄은 이번에 생성하지 않았다.
6. 기존 `/api/pay/webhook`은 결제 조회로 토스 상태를 확인한다. 테스트 웹훅 재전송과 취소 누락 복구를 검증해야 한다. 네트워크 시간 초과 후 새로운 요청 키로 다시 취소하지 않는다. 자동 POST 재시도는 최초 전송 후 24시간까지만 허용하며 이후 주인장 확인이 필요하다.
7. 자동 결제수단은 카드·간편결제·계좌이체를 대상으로 한다. 가상계좌 환급계좌, 결제수단별 취소 제한, 지급 여력 부족 등은 자동 완료로 간주하지 않는다. 토스 관리자에서 처리한 경우 완료 거래를 대조한다. 기존 미추적 잔액이나 예약되지 않은 외부 취소의 수동 정산 도구는 추가 작업이 필요하다.
8. 환불 검토함은 고객 알림 발송 시스템이 아니다. 고객은 내역에서 답변과 상태를 확인한다. 오래된 요청과 오류를 정기 확인하고 법정 환급 기한을 넘기지 않도록 운영한다. 페이지 목록은 최근 구매 100건/요청 200건이며, 더 많은 이력의 검색·페이지 나누기는 후속 작업이다.
9. 개정 약관의 실제 시행일, 회사 정보, 구매 전 안내와 미리보기 등 법정 조치, 문의 담당자를 확정한다. 활성화 후 새 주문에는 `purchase-refund-v2` 동의 버전을 기록한다. 확인란만으로 모든 환불 권리를 제한하지 않는다.

## 검증 범위

- `npm run test:refunds`: 실제 SQL을 PGlite PostgreSQL에서 실행. 유·무료 출처, 마이그레이션 재실행, 일부 환불·반올림, 잔액 보관, 중복 신청, 과다 환불 거절, 같은 출처 복원, 결과 권한 회수, 무료 이용 유지, 권한 제한을 검증한다.
- 결제 처리기는 위 SQL과 가짜 토스 응답을 함께 사용하여 응답 유실, DB 저장 실패, 재전송, 중복 웹훅, 외부 취소 대조, 결제 정보 불일치 등을 검증한다. 승인 화면 이후 사용으로 금액이 달라지면 거절되는 것도 검증한다.
- `tests/refund-http.mjs`: 실제 Next 빌드 서버와 가짜 Supabase를 이용해 로그인·관리자·출처·입력 검증과 금액 조작 방지를 확인한다. 테스트 빌드는 `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:3110`이 필요하다. 실제 토스 요청을 만들지 않는다.
- 모바일 시안: 360·390·430px에서 가로 잘림, 고객 신청과 3,267원 취소 완료, 관리자 승인/사유 안내, 지연 내역 재확인, 화면 전환을 점검했다.

**운영 활성화 전 추가 검증:** 토스 테스트 키로 실제 승인·전액/부분 취소 및 웹훅을 확인하고, Supabase 테스트 DB의 서로 다른 연결에서 환불/사용 동시 요청을 검증해야 한다. 현재 테스트는 실제 결제망 또는 운영 Supabase 검증을 대신하지 않는다.

참고: [토스 취소 연동](https://docs.tosspayments.com/guides/v2/cancel-payment), [토스 인증·멱등키](https://docs.tosspayments.com/reference/using-api/authorization), [전자상거래법 청약철회](https://law.go.kr/LSW/lsLawLinkInfo.do?chrClsCd=010202&lsId=009318&lsJoLnkSeq=1000527255&print=print).
